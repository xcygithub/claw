# 复制为 .env 并填入你的密钥。Claw 通过 litellm 调用模型，支持多家厂商。
# 关键规则: litellm 用模型名里 "/" 前面的前缀决定调用哪家服务。
#   - openai/xxx      -> 用 OpenAI 兼容客户端, 打 CLAW_API_BASE 指定的地址(中转/百炼兼容模式等)
#   - dashscope/xxx   -> 阿里 DashScope 原生
#   - anthropic/xxx   -> Anthropic
# 不带前缀(如 qwen-plus) 或写成厂商名(如 MiniMax/xxx) 会导致路由错误或 Missing credentials。

# ---------------------------------------------------------------------------
# 方式 A: OpenAI 官方
# ---------------------------------------------------------------------------
# OPENAI_API_KEY=
# CLAW_MODEL=gpt-4o

# ---------------------------------------------------------------------------
# 方式 B(推荐, 最通用): 任意 OpenAI 兼容端点(第三方中转 / 阿里百炼兼容模式 / 本地 vLLM)
# 用 openai/ 前缀 + 下面两个变量显式指定地址与 key。
# 例: 通过阿里 dashscope(百炼)兼容接口调用其上托管的 MiniMax 模型
# ---------------------------------------------------------------------------
CLAW_MODEL=openai/MiniMax/MiniMax-M2.7
CLAW_API_KEY=
CLAW_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
# 国内用 dashscope.aliyuncs.com; 国际版用 dashscope-intl.aliyuncs.com
# 注意: litellm 用"第一个 /"判断厂商, 所以前缀必须是 openai/。
# openai/ 后面填百炼控制台上的完整"模型 Code"(原样保留其自带前缀),
# 例如 MiniMax/MiniMax-M2.7、qwen-plus、deepseek-v3 等。

# ---------------------------------------------------------------------------
# 方式 C: 阿里 Qwen 原生 DashScope provider
# ---------------------------------------------------------------------------
# CLAW_MODEL=dashscope/qwen-plus
# DASHSCOPE_API_KEY=
# DASHSCOPE_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1

# ---------------------------------------------------------------------------
# 方式 D: Anthropic Claude
# ---------------------------------------------------------------------------
# ANTHROPIC_API_KEY=
# CLAW_MODEL=anthropic/claude-3-5-sonnet-20241022

# --- 其他运行参数 ---
# 是否自动批准所有工具调用(危险操作也不再询问)。生产慎用。
CLAW_AUTO_APPROVE=false

# 单次回复的最大工具调用迭代轮数
CLAW_MAX_ITERATIONS=25

# 触发上下文压缩的 token 阈值(占模型上下文窗口的比例, 0-1)
CLAW_COMPACT_THRESHOLD=0.7
